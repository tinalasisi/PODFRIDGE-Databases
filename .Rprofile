# Set CRAN mirror for all R sessions in this project
options(repos = c(CRAN = "https://cloud.r-project.org/"))

# Print message on startup
.First <- function() {
  cat("\nPODFRIDGE-Databases R environment loaded\n")
  cat("CRAN mirror set to: https://cloud.r-project.org/\n\n")
}
